# Mongodb docker

Using the mongodb docker images

```
git clone git@bitbucket.org:rajanikant-devmore/mongo-docker.git
cd mongo-docker
mkdir data
```

## Run the MongoDB

```
# Run the mongo
docker-compose up 

# Stop mongo 
docker-compose down
```